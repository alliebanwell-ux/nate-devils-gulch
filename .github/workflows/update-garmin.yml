name: Update Garmin Tracker

on:
  workflow_dispatch:
  schedule:
    - cron: "*/10 * * * *"

permissions:
  contents: write

concurrency:
  group: garmin-tracker-update
  cancel-in-progress: false

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Download Garmin KML
        env:
          GARMIN_KML_URL: ${{ secrets.GARMIN_KML_URL }}
        run: |
          test -n "$GARMIN_KML_URL"

          curl \
            --fail \
            --location \
            --silent \
            --show-error \
            --retry 3 \
            --retry-delay 3 \
            --connect-timeout 20 \
            --max-time 60 \
            --header "Accept: application/vnd.google-earth.kml+xml,application/xml,text/xml,*/*" \
            --user-agent "Mozilla/5.0 DevilGulchTracker/1.0" \
            "$GARMIN_KML_URL" \
            --output feed.kml

          test -s feed.kml
          grep -qi "<kml" feed.kml

      - name: Update tracker data
        run: |
          python scripts/update_garmin.py \
            --kml feed.kml \
            --race race.json \
            --course course.json

      - name: Commit updates
        run: |
          rm -f feed.kml

          if git diff --quiet -- race.json course.json; then
            echo "No new Garmin point."
            exit 0
          fi

          git config user.name "garmin-tracker-bot"
          git config user.email "actions@users.noreply.github.com"

          git add race.json course.json
          git commit -m "Update Garmin race position"
          git push